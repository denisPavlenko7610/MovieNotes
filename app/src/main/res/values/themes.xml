<resources>
    <style name="Theme.MovieNotes" parent="Theme.Material3.Light.NoActionBar">
        <!-- Цвета можно настроить здесь или оставить по умолчанию -->
    </style>
    <style name="WhiteText">
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:textSize">18sp</item> <!-- Обычно для текста лучше sp, 36sp — очень большой -->
        <item name="android:maxLines">5</item> <!-- Максимум 5 строк, можно больше -->
        <item name="android:ellipsize">none</item> <!-- Не обрезаем текст с троеточием -->
    </style>
</resources>